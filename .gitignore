# dependencies / build
node_modules/
.next/
out/
build/
*.tsbuildinfo

# secrets — NEVER commit (Proxmox creds, forwarding secret, live state)
.env
.env.*
!.env.example
dashboard/data/
**/data/conduit.json

# local scratch / archives (seed assets live in the conduit-assets repo)
*.tar.gz

# misc
.DS_Store
*.log
.vercel
